<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
	xmlns:android="http://schemas.android.com/apk/res/android"
	android:layout_width="fill_parent"
	android:layout_height="fill_parent">
		<TextView android:id="@+id/DownloadShowsLabel"
			android:text="@string/downloaded_shows_text"
			android:textColor="#127DD4"
			android:layout_width="fill_parent"
			android:layout_height="wrap_content"
			android:textSize="24sp"
			android:textStyle="bold"
			android:gravity="center_horizontal"
			android:padding="6dip"></TextView>
		<ListView android:id="@+id/DownloadedListView"
			android:layout_width="fill_parent"
			android:layout_height="fill_parent"
			android:layout_below="@+id/DownloadShowsLabel"
			android:layout_alignParentBottom="true"
			android:background="#000000"
			android:overScrollFooter="@null"></ListView>
</RelativeLayout>